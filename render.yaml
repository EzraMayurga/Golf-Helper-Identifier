services:
  - type: web
    name: golf-helper-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PYTHON_BACKEND_URL
        value: http://localhost:8000
      - key: JWT_SECRET
        generateValue: true
